# Create dataset with user-owned mountpoint
Create a dataset for myuser's homedir and set the userid and group id of the dataset so they have access. 
(This assumes that a zpool named 'dpool' already exists)